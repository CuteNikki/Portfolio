import { ABOUT_METADATA } from '@/constants/metadata';

import { AboutContent } from '@/components/pages/about';

export const metadata = ABOUT_METADATA;

export default function AboutPage() {
  return <AboutContent />;
}
